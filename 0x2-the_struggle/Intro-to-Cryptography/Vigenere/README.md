# Vigenere Cipher Decryption/Encryption Tool

```shell
usage: vigenere.py [-h] [-e] [-d] [-i INPUT_FILE] [-o OUTPUT_FILE] [-v]

Vigenère cipher decryption tool.

optional arguments:
  -h, --help            show this help message and exit
  -e, --encrypt         Encrypt the input file
  -d, --decrypt         Decrypt the input file
  -i INPUT_FILE, --input-file INPUT_FILE
                        The file from which the text will be read to be encrypted or decrypted
  -o OUTPUT_FILE, --output-file OUTPUT_FILE
                        The file where the output will be saved
  -v, --verbose         Enable verbosity
```
